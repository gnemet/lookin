# `<Feature>` — Brief (BRD)

> The **why**. One page max. Goals carry IDs (`G#`) that EARS clauses in
> `requirements.md` trace back to. **Mandatory for every new spec** — infra /
> library work included; keep it compact there (problem, goals, scope in a few
> lines). Existing brief-less specs are grandfathered, not backfilled.
>
> Placeholders are written in backticks (`` `<role>` ``) — a bare `<role>` is parsed as an
> HTML tag and silently dropped by every Markdown renderer.

## Problem
*What hurts today, in business terms. Not the solution.*

## Stakeholders
- **`<role>`** — *who they are, what they need*

## Goals
- **G1 — `<short name>`.** *one sentence* → *which req IDs realize it*
- **G2 — …**

## Success metrics
- *observable, testable outcome — how you'll know it worked*

## Scope
- **In:** `<…>`
- **Out (deferred):** `<…>`

## Constraints
- *platform rules, security boundaries, things that must NOT happen*
