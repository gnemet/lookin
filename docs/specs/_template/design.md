# <Feature> — Design (enforcement map)

> The **how**. For each requirement in `requirements.md`, the mechanism that
> enforces it and the artifact that verifies it. One row per requirement ID.
> A `[~]` / `[ ]` requirement names the gap; the work item lives in `tasks.md`.

## Context
<1–3 sentences: where this fits, the load-bearing design bet.>

## Mechanism map
| Concern | Mechanism (file / type / function) | Requirements |
|---|---|---|
| <concern> | `<path>` / `<symbol>` | U1, E1 |

## Acceptance map
| Req | Mechanism | Verification (how proven) |
|---|---|---|
| U1 | <mechanism> | <test / smoke / build that proves it> |
